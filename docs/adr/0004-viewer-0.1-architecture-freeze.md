# ADR 0004: Viewer 0.1 architecture freeze

- Status: Proposed
- Date: 2026-07-16
- Gate: G4 architecture freeze
- Reproduce: `./scripts/run-architecture-gates.sh`

## Context

G1 through G3 tested the highest-risk Viewer 0.1 assumptions before product milestone implementation: native image rendering, recoverable filesystem mutation, progressive traversal, disposable indexing, Unicode search, stale-work rejection, and filesystem reconciliation. G4 converts those prototype results into one auditable dependency, security, performance, and cross-crate API baseline.

No product feature is accepted by this decision. M1 through M4 may start only after this record is accepted and all architecture gates pass from the frozen dependency graph.

## Gate evidence manifest

All rows use the standard development device: MacBook Air `Mac16,12`, Apple M4 (10 cores, 24 GB RAM), Apple Silicon `aarch64`, macOS 26.5.2 (`25F84`). A checksum identifies the deterministic corpus generator or committed fixture manifest used by the gate; G2's corpus is generated by its committed integration-test harness.

| Gate | Accepted commit | Command | Corpus evidence (SHA-256) | Performance evidence | Peak RSS | Selected outcome | Raw report / review |
| --- | --- | --- | --- | --- | --- | --- | --- |
| G1 image pipeline | `5aee3ab` | `./scripts/run-g1-image-gate.sh` | `tests/fixtures/images/manifest.json`: `a0fcc3ef292006d469ad5367b06fecb588dbe4f3cfc54acdeccce3acca746242` | worst repeated thumbnail p50/p95 37.16/40.59 ms; worst fit-preview p95 15.46 ms | 112.4 MB | Quick Look grid thumbnail with Image I/O fallback; Image I/O fit/approved 100% preview | Generated `target/g1-image-gate/benchmark-report.json`; [ADR 0001](./0001-macos-image-pipeline.md); [review](../reviews/2026-07-16-g1-image-pipeline-review.md) |
| G2 file transaction | `fa18c11` | `./scripts/run-g2-file-transaction-gate.sh` | `tests/file_transactions.rs`: `4469ff65992628cd4d44898939abf463d679575b1752e3b6fef56ecd7ab78cc9`; disposable cases are generated at runtime | Latency percentiles not applicable to this correctness/crash-recovery gate | Not separately measured; bounded 1 MiB copy buffer | Durable seven-state journal, verified copy, no-replace rename, Trash adapter, conservative recovery | Console test report regenerated by gate; [ADR 0002](./0002-file-transaction-protocol.md); [review](../reviews/2026-07-16-g2-file-transactions-review.md) |
| G3 scan/search | `0ba17dd` | `./scripts/run-g3-scan-search-gate.sh` | `scripts/generate-g3-project.sh`: `a4dc47e9c9e4a4bd860d9efd9cebdc66a0ffcd8da918566d3c2c814cc47eb80f` | folder p50/p95 20.07/20.08 ms; scan 38.72/39.17 ms; search 0.66/0.86 ms | 6.67 MB | Folder-first bounded scan, WAL session index, FTS5 trigram + nucleo, generation-safe publication | Generated `target/g3-scan-search-gate/benchmark-report.json`; [ADR 0003](./0003-scan-search-and-generation.md); [review](../reviews/2026-07-16-g3-scan-search-review.md) |

Generated files under `target/` are reproducible evidence and are intentionally not versioned. The accepted ADRs and reviews retain the selected result, environment, sample count, budgets, and limitations.

## Provisional freeze outcome

G1–G3 are accepted individually. This G4 record remains proposed until locked dependencies and licenses, the Tauri boundary, the documented public API surface, and the complete Viewer 0.1 requirement-to-milestone matrix have each passed their executable checks.

## Acceptance conditions

This ADR may move to `Accepted` only when:

1. `scripts/check-locked-dependencies.sh` passes without changing either lockfile;
2. `scripts/check-tauri-security.sh` proves the negative path, protocol, Markdown, capability, CSP, and navigation boundaries;
3. the public Domain/Application API inventory matches generated documentation and the reconciled architecture specification;
4. every confirmed product requirement maps to one M1–M4 owner and acceptance test; and
5. `scripts/run-architecture-gates.sh` and the Apple Silicon release build both pass.
