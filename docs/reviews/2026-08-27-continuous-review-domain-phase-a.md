# 持续评审阶段 A：Domain 实施与验证记录

> Status: Development evidence
>
> 日期：2026-08-27
>
> 当前状态：阶段 A（任务 1–5）完成；独立只读复审与最终门禁通过。

## 范围与隔离

- 用户授权创建独立 worktree，当前任务内逐项实施阶段 A，不并行分派实现任务。
- 基线 `d7abb9961f377ae257c3283ef7893218ec0c53f9`；分支 `codex/continuous-review-domain`。
- 工作区 `/Users/abc/Project/Viewer/.worktrees/continuous-review-domain`；原分支未改动、未合并。
- 只新增 Domain 纯规则、测试和工程文档；旧素材／Anchor 校验仅扩大模块内部可见性。
- 没有 UI、协议、存储、迁移、Agent 读取入口变更；没有操作用户的“下载/测试图”素材。
- 签名、公证、正式安装包、上架、发布与发售不在本轮范围，也不是完成条件。

## 已实现规则

1. 稳定身份、独立文字／目标版本、空当前状态、资源和引用验证。
2. 原文保真编辑、指定目标重绘、共享意见部分撤回；无变化不制造快照。
3. 精确依据存档，保留后补文字／标注／新增内容；已删除目标不复活；有效覆盖去重。
4. 明确选择恢复，保护未选目标及新意见；继续提出使用新身份、历史引用和适用性确认。
5. 独立源检查投影、内容净增量和明确移出原因；不向 Agent 混入旧全文或逆向指令。

实施细化与后续接口约定见[计划第 3 节](../superpowers/plans/2026-08-27-viewer-continuous-review-and-agent-handoff.md)
和 [ADR 0006](../adr/0006-continuous-review-snapshot-and-archive-protocol.md)。

## 验证证据

- worktree 初始 `cargo test --locked -p viewer-domain`：旧 44 项通过。
- 每个任务均先运行新测试，确认缺失新类型／行为导致 RED，再实现并运行 GREEN。
- `7aec1e5`：Domain 103 项通过（新增 59）；`cargo clippy --locked -p viewer-domain --all-targets -- -D warnings` 通过。
- `7aec1e5`：`pnpm verify:clean` 退出 0；日志 `/tmp/viewer-continuous-domain-verify.q5tJSV/verify.log`。
  UI 132 文件、1,142 项通过、1 项既有跳过；协议 28 项通过，旧闭环与架构／安全门禁通过。
- `524ddf5`：补充 3 个先失败后通过的边界回归；聚焦测试与严格 Clippy 通过。
  修复历史-only 存档漏验新状态、未知依据伪造非当前键和历史意见归属不一致。
- 独立审阅通过技能派发只读 reviewer，不分派实现、不改分支；对 `1f98dd2` 的复验确认
  两项 Important 均已关闭，修复范围内未发现具体回归，阶段 A 检查点可通过。
- 独立审阅发现两项 Important：增量链在缺席区间丢失目标归属，以及批量恢复复制共同意见。
  前者已在 `311b53f`／`1f98dd2` 加失败回归并修复；后者已在 `d470d63` 改为共享不可变载荷，
  以指针共享断言和 10,000 目标恢复回归核验，不用脆弱的运行时间阈值代替正确性断言。
- `1f98dd2`：Domain 111 项通过（新增 67）；严格 Clippy 通过。
- `1f98dd2` 代码基线与本轮工程文档：最终 `pnpm verify:clean` 退出 0；日志
  `/tmp/viewer-continuous-domain-final.q7A4ZL/verify.log`。UI 132 文件、1,142 通过、1 既有跳过；
  Node 政策 33、协议 28、旧闭环 7、视频运行时测试 30、门禁自测 11 均通过；
  Rust 74 个结果块合计 940 次通过、0 失败（跨门禁调用的累计次数，不宣称 940 项独立测试）。
  架构边界、Tauri 安全边界、依赖与许可证检查通过。随后只更新收口文档并复跑文档政策门禁。
- Minor 后续约束：捕获的 AssetVersion 不原地改变路径／mtime；已确认实时定位映射归属在
  Task 10 明确，不能把改名伪装成新产出版本。已同步计划与 ADR，未提前实现映射。

上述结果不等于 UI／原生新流程已经验收。临时日志不属于长期产品数据，长期依据是提交、
测试代码与本记录；后续代码变化仍需按同一命令重新验证。

## 提交与续接

| 提交 | 范围 |
| --- | --- |
| `92b1836` | Task 1：模型与验证 |
| `fb85ea2` | Task 2：新增、编辑与撤回 |
| `0a8d6b7` | Task 3：精确存档 |
| `ae8b242` | Task 4：恢复与继续提出 |
| `7aec1e5` | Task 5：投影与净增量 |
| `524ddf5` | 自检发现的存档边界校验补齐 |
| `311b53f` | 增量缺席区间保留目标归属 |
| `d470d63` | 批量恢复共享载荷与索引合并 |
| `1f98dd2` | 历史身份复用与共同意见恢复回归 |

下一阶段为 B（Tasks 6–10），从 v3 wire 契约和持久化开始，不提前切换 UI。
阶段 A 已收口，等待用户确认下一阶段；本轮不合并原分支，也不迁移现有工程。
