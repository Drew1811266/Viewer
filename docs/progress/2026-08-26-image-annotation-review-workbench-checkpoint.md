# 图片标注评审工作台：开发检查点

> 日期：2026-08-26。状态：Development evidence；Task 14 尚未完成。

最新续接：用户已接受当前工作台为首版视觉基准，并明确允许本机 Playwright 捕获。
跨层复核的 8 项问题已修复于 `ee71acc`；验收脚本增量 `4366312` 也已通过独立聚焦复核。
两种尺寸、18 项修复后完整成对视觉检查已通过。已确认截图、摘要和验证范围见
[首版基准确认记录](../reviews/2026-08-26-image-review-visual-baseline.md)。
设计 §13.4 的原生高分辨率完整流程仍待可操作的开发版窗口及测试图片目录，产品文档和
最终收尾仍未完成。此前 `2d68d3d` 的完整验证仅是历史证据，不能替代后续提交的验证。
不要重复询问视觉方向或已取得的捕获许可。

## 续接位置

分支为 `codex/image-annotation-review-workbench`。继续执行
[已确认的实施计划](../superpowers/plans/2026-08-26-viewer-image-annotation-review-workbench.md)，
不要重新启动产品方向讨论。Tasks 1–13 已实现并逐项提交；Task 14 的功能检查和
验收场景已实现，首版视觉方向、完整状态基准及修复后视觉对照已确认；
剩余原生流程、产品文档同步及最终收尾验证不得提前标为通过。

Viewer 仍处于开发初期。代码签名、Apple 公证、正式安装包、公开发布和发售均不属于
本轮开发、验收、阻塞或收尾工作，也不是每次功能完成后的默认任务。

## 已完成提交

| Task | 内容 | 提交 |
| --- | --- | --- |
| 1 | 有效的整图、矩形、画笔 Anchor 与限制 | `14c8550` |
| 2 | 隔离 v1 协议编解码，保留历史兼容 | `4c0cb4a` |
| 3 | v2 开放评审协议 | `1cd6f92` |
| 4 | 带版本、摘要和安全位置的目录记录 | `cdab9fd` |
| 5 | 崩溃一致的评审包发布和恢复 | `bc047c1` |
| 6 | 受限标注 PNG 渲染 | `b64b177` |
| 7 | 首条意见原子建 Draft 与标注用例 | `a12d624` |
| 8 | Tauri DTO 与前端桥接 | `555d792` |
| 9 | 不依赖 Review 的复用图片视口 | `c35fd05` |
| 10 | 前端标注编辑状态机 | `e7eadab` |
| 11 | 图片标注工作台 | `d301852` |
| 12 | 工作区、固定范围和网格反馈集成 | `d11ba3d` |
| 13 | 外部 v1/v2 Reader 和端到端兼容 | `76e5d0c` |

## 本次续接：Task 14

新增 RVW-16–RVW-24 九个确定性验收场景，覆盖自动开始、同图四条意见、保存失败、
矩形编辑、画笔重绘、范围外只读、未保存离开保护、网格意见计数和 200% 缩放。
场景经过真实 App、工作台和协调器，只有后端桥接使用内存夹具，不是截图替身，也不等于原生验收。
权威场景目录共 128 项，其中 24 项为评审场景。A11Y-05 原有 200% 缩放保持不变。

浏览器检查发现并修复：

- 工具栏在窄窗口重叠；评审专用工具栏允许换行，不改变普通预览布局。
- 200% 缩放时逻辑宽度与窗口宽度不同；意见栏按真实工作台宽度折叠。
- CSS 缩放下指针坐标映射错误；在通用图片投影边界修正，并覆盖四种旋转。
- 编号与矩形手柄重叠，以及偏移后的编号在图片外无法拖动；只为已有矩形编辑
  开放图片外坐标投影，新建标注仍拒绝图片外起点，最终几何仍限制在图片范围内。
- 底部翻页与意见栏/图片重叠；为翻页留空间并按图片区居中。
- 未保存区域在输入或保存失败时消失；保留虚线草稿，失败时恢复文字输入焦点。
- 短视口的就地编辑器超出图片区；使用紧凑输入布局并允许编辑器内部滚动。
- 首条意见夹具的范围数不一致；RVW-16 从当前目录的 30 张图片建立固定名单，
  单元测试核对 proposal、完整成员名单和保存后的总数。其余既有 Draft 场景保留六个固定成员。

独立代码复核最初发现一项拖动回归和一项夹具不一致：前者已由失败测试、修复后测试及
浏览器复测确认修复；后者已补充失败测试并修正。没有遗留的已确认 Critical/Important 发现。
该复核只覆盖当时的修正，不等于完整验收通过或可以合并；后续跨层复核的新发现见下文。

## 首次检查的已验证范围（历史记录）

- 前端静态检查、完整单元测试与生产构建通过。最近完整测试：130 个文件，1123 项通过，1 项跳过。
- 视觉验收脚本单元测试 17 项通过；这不是运行视觉截图对照脚本的结果。
- 开放协议测试 28 项、手工轮次端到端测试 7 项通过。
- Rust 评审 Domain、Application 的开始/修改/完成/目录、Infrastructure 的协议/仓库/素材、
  macOS 图片渲染和 Desktop 评审命令测试均通过。
- 架构依赖检查及跨层契约测试通过；政策检查 33 项通过，范围检查覆盖 48 条要求、13 条冻结决定。
- 架构趋势命令退出码为 0，最终复测有 21 条非阻塞告警，包括大文件、长函数、分支复杂度及
  测试/生产代码比例下降。没有改动阈值或把告警视作已经解决。
- Biome 的既有配置弃用提示、JSDOM canvas 提示和生产包大于 500 kB 的提示仍存在。

本节记录首次检查时的范围。当时尚未执行完整 `pnpm verify:clean`；后续已在 `2d68d3d` 上通过，
详见首版基准确认记录。设计尚未标为 `Implemented`，计划也未标为 `Complete`。

## 早期浏览器证据（已由完整捕获补齐）

证据根目录：
`target/viewer-visual-acceptance/image-annotation-review-workbench/browser-product-evidence/`。
每个 `<viewport>/RVW-<编号>/` 目录保存 `product.jpg` 和 `manifest.json`。
这些文件是忽略目录中的本机开发产物，不随 Git 提交；丢失时须重新捕获，不应只凭路径宣称通过。

九个场景在 `1024x720`、`1440x900` 下均达到语义 ready、无待完成 UI 任务，
且工具栏可见控件通过边界与互不重叠检查。1024 宽的产品截图已逐张检查。
另实测了 200% 下的新建标注、保存失败焦点和未保存离开保护，以及缩小后的编号拖动。

首次检查时发现以下两项；第一项的视觉方向现已由用户确认，完整状态基准仍须建立：

1. 没有找到已确认的 RVW-16–RVW-24 参考截图。现有 atlas 不包含这组新工作台状态，
   文字设计不是像素参考图。不得把本次产品截图复制成参考图后宣称比对通过。
2. 当前内置浏览器的 1440×900 DOM 视口正常，但截图返回 1372×900 JPEG，右侧被裁切；
   `fullPage`/显式裁切参数的替代尝试出现拼接异常。宽窗口只记运行时检查通过，完整截图未通过。

截图实际格式为 JPEG，文件后缀已如实使用 `.jpg`，并在 manifest 记录实际截图尺寸与限制。
当时没有有效的 `reference.png` / `combined.png` 成对证据，因此没有运行并宣称
`pnpm accept:visual` 全部通过，也没有把浏览器夹具结果描述为原生 macOS 端到端验证。

## 后续完整基准与跨层复核

用户明确允许本机独立 Playwright 后，完整捕获复现了 RVW-23 就绪信号过早的问题。
`8009656` 修复两帧期间的语义就绪重检，新增回归先失败后通过；22 项聚焦测试、
17 项脚本测试及前端构建通过。

该干净提交的 RVW-16–RVW-24 在两种尺寸下共 18 张首版参考已冻结，并通过另一次独立
`pnpm accept:visual` 捕获与并排人工检查。无页面/控制台错误、裁切或布局回归。
完整尺寸、摘要及参考来源均已核对，证据位置和比较细节见
[首版基准确认记录](../reviews/2026-08-26-image-review-visual-baseline.md)。

完整分支复核另发现 8 项 Important、0 项 Critical：

1. v1 区域意见完成时错误进入 v2 衍生 PNG 流程。
2. 原生标注 PNG 的源图方向与 Anchor 可能上下不一致。
3. 极小或细长图片的编号约束可能导致渲染失败。
4. 已有文字编辑、进行中绘制及保存失败几何未统一纳入离开保护。
5. 普通预览快捷键抢占评审文字输入。
6. 连续新建画笔可能被解释为重绘上一条意见。
7. 删除恢复及同时间戳意见的界面/导出编号可能不一致。
8. Rust v2 发布、读取及恢复的资源包校验弱于外部 Reader。

上述问题已由一次集中修正及一次聚焦复核处理完毕，不能用此前全绿的测试结果掩盖其发生。

## 修正后的当前证据

- `ee71acc`：修复上述 8 项问题；实际 PNG 像素/EXIF 方向、极小图片、v1 真实仓库完成、
  真实工作台编辑保护/快捷键/连续画笔、编号一致性及非法资源包发布/恢复均补了失败回归。
- `4366312`：只修正 RVW-20 保存后状态的验收判断。它验证桥接返回的真实已保存路径改变，
  不是仅凭模拟了拖动就宣称保存成功；5 项回归先失败后通过，验收测试 91 项通过。
- 独立聚焦复核覆盖两份增量，8 项全部解决，没有遗留或新增 Critical/Important 发现。
- 修复实现者完整测试：UI 131 文件、1137 通过/1 跳过；Rust 852 通过/0 失败/7 既有忽略；
  协议 28、服务端轮次端到端 7、政策 33 通过；范围仍为 48 条要求/13 条冻结决定。
  这是 `ee71acc` 的结果；`4366312` 另增加 5 项验收测试，不将旧数量描述为其完整套件结果。
- UI 检查、生产构建、Rust 格式、严格 Clippy 及架构边界通过。日志为本机 `target/fix-*.log`。
- 架构趋势为 24 条非阻塞告警，较首版增加 3 条：工作台控制器函数 287 行、v2 资源校验
  分支分数 19、就地编辑器分支分数 16。没有调整阈值或分类以消除告警。
  既有 Biome 配置、JSDOM canvas、构建体积及依赖重复版本提示仍存在。
- 干净 `4366312` 的 18 张产品/冻结参考并排图逐张检查通过，无页面/控制台错误。
  12 张逐像素一致，其余为未保存编辑保护的按钮禁用，以及重绘后回到浏览状态；
  具体差异与证据路径见首版基准确认记录。旧 `ee71acc` 的 RVW-20 两张失败截图保留为失败记录。

本轮作出的兼容/交互裁定及代价：

1. v1 完成不自动升级、不生成 v2 PNG，以遵守旧协议字节兼容；若期待旧轮次也有预览图，
   代价是需另行明确升级，不能静默改写。
2. 编号统一按创建时间加 Feedback ID 投影，不重排 v1 持久化字节；代价是某些恢复草稿的
   临时显示顺序可能变化，但意见身份、文字及界面/导出对应关系不丢失。
3. 明确重绘保存成功后回到共享浏览状态；规格没有要求持续画笔模式，代价是继续画下一条
   时需要再次按 B 或点击画笔。验收脚本应核对真实保存结果，不保留旧绕行行为。

## 原生流程的实际阻塞

设计 §13.4 要求真实高分辨率 30 图的完整界面旅程，不能由 18 个浏览器夹具或服务端 e2e
代替。当前生产 UI 和原生代码已使用开发配置成功构建并启动 `target/debug/viewer-desktop`；
没有生成正式安装包或改变构建源配置。原生操作工具明确返回 Mac 已锁定，需要用户手动解锁。
尚未执行任何原生界面验收步骤，也未通过自动化打开/修改用户项目。

高分辨率素材目录尚待用户提供。独立烟雾测试副本位于
`target/review-workbench-native-Rr0MXU/评审验收副本/`，来自仓库的 30 张 560×373 图片，
源文件和副本 SHA-256 均已核对不变；这不是高分辨率验收。忽略目录中的准备脚本与后验断言
分别为 `target/review-workbench-native-fixture.mjs` 和 `target/review-workbench-native-result.mjs`。
后验断言尚未针对真实完成结果运行，不得据脚本存在宣称流程通过。

后续主机于 22:35 唤醒，应用枚举不再返回锁屏错误，但没有找到 Viewer；以配置中的
`com.viewer.desktop` 获取窗口返回 `Invalid app`。精确的开发版进程仍在运行。
这是原生 UI 自动化定位未就绪，不是原生功能已验收，也不能继续把早期锁屏当作唯一当前原因。
没有绕过 UI 工具改用其他输入注入方式。

依设计 §14，保持产品文档原有事实边界及计划/设计 Active 状态；在上述原生证据补齐前，
不将本轮整体写为已实施或完成。

## 检查点完整验证中的休眠中断

干净 `f57b24c` 上的 `pnpm verify:clean` 未通过，原始日志保留为
`target/review-workbench-checkpoint-verify.log`：UI 1024 通过、4 失败、1 跳过，另有
17 个工作进程启动错误；后续生产构建、完整 Rust 和安全检查尚未在这次运行执行。
不能把这次运行与其他日志拼接为一次完整通过。

已按系统性排错核对：UI 于 21:29:25 开始，macOS 电源日志在 21:29:31 进入空闲休眠，
其后多次短暂 DarkWake 后继续休眠，直到 22:35:19 才由用户活动唤醒。两个 5 秒限制的
测试实际分别记录约 1016 秒和 974 秒；一个纯同步按钮测试也记录约 1016 秒。
3 个失败是超时，第 4 个发生在同一 App 文件前一项超时之后。相关测试及配置没有被修复增量改动。

唤醒后，在同一提交上用仅作用于子进程生命周期的 `caffeinate -i` 重跑两个失败文件：
`App.test.tsx`、`ViewerControls.test.tsx` 共 96 项通过，耗时 5.50 秒。
日志为 `target/review-workbench-checkpoint-focused-awake.log`。没有延长超时、减少测试、
改动产品代码或长期电源配置。完整验证仍需重新从头执行，不能仅凭这次聚焦通过宣称完成。

## 下一步

1. 当前检查点提交用 `caffeinate -i pnpm verify:clean` 从头重跑，日志保存为
   `target/review-workbench-checkpoint-verify-awake.log`。独立架构趋势已退出 0（24 条告警），
   视觉脚本测试 17 项通过，日志为 `target/review-workbench-checkpoint-trends.log`、
   `target/review-workbench-checkpoint-visual-tests.log`。续接时核对实际输出和提交，
   不把日志路径本身当作通过证明。
2. 取得可操作的开发窗口及用户提供的高分辨率测试目录后，只复制素材到独立测试目录，完成设计 §13.4：
   第 6 张两条路径+两个矩形、网格 4 条、重开恢复、29 通过/1 返工/0 不可评审、Reader 校验。
3. 原生验收通过后，同步产品规格、使用指南、功能、快捷键、隐私、故障排查及变更记录。
4. 在最终实现/文档提交上执行完整质量、安全与 `pnpm verify:clean`，再更新设计和计划最终状态。

不要顺手扩展视频时间段/区域标注、AI Agent 联网集成、浏览曝光统计或发布工作。
