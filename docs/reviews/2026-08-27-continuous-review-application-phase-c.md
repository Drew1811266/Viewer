# 持续评审阶段 C：应用用例实施记录

> Status: Development evidence
>
> 日期：2026-08-27。阶段 C（任务 11–13）已通过完整门禁与独立复验；累计完成 13 / 23。
> 下一阶段为 D（任务 14–16）；当前不代表新流程已在 UI 中开放。

## 范围与基线

沿用批准的[实施计划](../superpowers/plans/2026-08-27-viewer-continuous-review-and-agent-handoff.md)，
在 `/Users/abc/Project/Viewer/.worktrees/continuous-review-domain`／`codex/continuous-review-domain`
推进任务 11–13。基线 `3098d3b`；阶段 B 的 46 项仓储／恢复／素材测试和 Application
27 项单元测试在开始前通过。原工作区和真实素材不在写入范围内。

## 任务 11：专项门禁通过

- 新增独立持续编辑、存档恢复、历史投影、证据准备、恢复输入和命令预算模块，不增加
  legacy `review_session` 的编排职责；未切换桌面组装、UI 或 Agent reader。
- 显式项目／Stream 上下文、预览时准备的素材版本、可取消 apply、提交后刷新失败回执，
  以及规范化命令摘要的接口补全见 [ADR 0006](../adr/0006-continuous-review-snapshot-and-archive-protocol.md)。
- 15 项应用测试覆盖连续保存、原文／共享文字版本、部分存档与恢复、旧依据保留新文字、
  来源变化待确认、历史继续提出、取消／写失败／过期输入恢复、回执丢失与乱序重试、
  当前空身份、非法输入、命令预算和文字修改复用标注像素。
- 真实 macOS catalog→原生捕获→Application→文件仓储串接通过：EXIF 6 正向尺寸、四处标注、
  部分存档／恢复、覆盖后的旧意见编辑与旧证据读取。只写 TempDir，不接触用户原图。
- 命令摘要 2 项、仓储 22 项、恢复 13 项、存档时间顺序负例 1 项通过。先观察失败再修正
  文字编辑重绘、待提交命令总量限制、单次祖先证明重复 IO；未放宽静态门禁。

### 存档成本量测

使用 `continuous_review_costs` 显式忽略测试，在独立临时工程构造一个素材、两个目标，反复
存档／恢复同一目标。Debug 构建，当前读取取 20 次均值；保存和 coverage 各一次。
这不是 UI 延迟、视频源扫描或大图片证据负载验收，不设易波动的时间断言。

| 存档数／测量时快照数 | 原保存 ms | 验证复用后保存 ms | 原 coverage ms | 复用后 coverage ms | 复用后当前读取 μs |
| --- | ---: | ---: | ---: | ---: | ---: |
| 10 / 22 | 71 | 32 | 29 | 8 | 372 |
| 30 / 62 | 412 | 62 | 206 | 24 | 458 |
| 60 / 122 | 1,418 | 112 | 708 | 51 | 663 |

日志：隔离工作区 `target/continuous-review-phase-c-costs-before.log` 与
`target/continuous-review-phase-c-costs-after.log`。图中只比较同一次开发环境运行，不能承诺
所有项目均有相同比例改善；历史遍历和大数据成本仍然有界且非零。

单元测试确认：重复证明中间祖先不重复读文件，所请求记录仍被重新核验；新 View 不继承信任，
请求文件损坏仍拒绝。已有 10,000 / 10,001 节点限制、未来依据伪造与故障恢复测试保持通过。

## 任务 12：专项门禁通过

任务 11 提交 `475677c`。新增 descriptor-relative 的显式声明读取和流式输出核验；
应用层保存有界候选，采用或带声明存档时重新核验源字节。仓储新增只读声明查询，采用与
新状态／存档保持单次索引原子提交。声明只核验版本依据，不宣称执行、解决或通过。

产出候选的错误与合法依据分开；保留原声明不等于确认关联。额外负例先复现“同一个 Target
已被重新绑定后，旧声明被错误用作新的版本关联证明”，再补实际依据快照的目标→素材核对。
参见 ADR 的任务 12 信任边界。正确映射仍必须经过显式目标与位置确认。

目前导入专项 8 项通过，包含 64 MiB 精确边界／超限、路径穿越、符号链接、未知字段、
错误上下文／摘要／版本、重复 ID 和异常产出。应用专项扩展至 21 项并全部通过。
真实 macOS 串接亦覆盖导入后磁盘声明被替换：拒绝采用、当前不变；恢复原字节后重试可提交，
四条待确认意见不被自动存档。测试仍全部使用 TempDir。

## 任务 13 与复审修正

任务 12 提交 `ef4df73`。任务 13 现有 10 项真实临时仓储迁移测试通过；应用测试扩展至
24 项，仓储 22／恢复 13／声明 8 项及 Node 协议 30 项保持通过。严格 Clippy 通过。
迁移逐条验证旧记录／PNG，绑定索引及草稿的复合确认摘要，保留可核验备份；覆盖仅保留历史、
选择部分旧目标、活动草稿、v1/v2 混合、损坏证据、六个提交故障点、重复迁移、只读、
未知版本、空目录状态和中断后的多份备份。未改变真实项目。
新接口和保守底图规则见 ADR 0006。历史继续提出使用旧目标引用，不伪造 v3 目标版本。

任务 13 提交 `30cdeee`。独立只读审阅发现的问题已据实复现并修正，最终复验结论见文末：

- 历史继续提出、旧意见继续提出、重新绑定、同版本位置确认和恢复为新意见都保存原始区域与精确来源选择；
  未确认区域不伪造几何。外部声明核验等早期失败也保留输入。
- 迁移取消／失败时保存私有 MigrationPlan（复合 inspection digest、选中旧目标、新素材和区域绑定）。
  重启后可以读到恢复输入，旧索引不变；不自动重放、不把草稿作为 Agent 要求。
- 待执行使用声明绑定规范内容、源字节和相对路径；重启后导入同 ID 不同内容不能替换旧命令含义。
- 同素材版本的适用性确认只解除单一 ApplicabilityUnconfirmed；不解除缺失底图或源文件变化限制。
- 历史按精确快照合并读取并限制总内存估算；恢复核对采用一次历史遍历；同一旧记录的 100 个引用
  从 100 次完整读取降为每次操作 1 次，仍核验声明的 PNG，新操作不复用信任。
- 缺失索引但存在旧 Draft 时普通 writer 同样拒绝；迁移重试先查已提交回执再处理取消；
  `legacyIndex: null` 与 Schema 一致拒绝。

失败先行的新增回归包括：跨重启替换声明、同版本待确认无法解除、重复完整历史克隆、
旧引用重复 IO、缺索引绕过、迁移取消丢选择，以及渲染前声明失败丢区域绑定。
接口落地和约束见 ADR 0006 的 Phase C review corrections。

任务 11 的 Application 全量、仓储 22 项、architecture:boundaries 通过；Application、
Infrastructure、macOS 全目标严格 Clippy 通过。边界日志为
`target/continuous-review-phase-c-task11-boundaries.log`。测试模块位置的一项 Clippy 错误已按
既有规范拆为独立测试文件后复验，不增加豁免。

## 完整验证

第一轮修正（`7fae416`）的 `pnpm verify:clean` 已退出 0，验证没有改变工作区条目。
完整日志：隔离工作区 `target/continuous-review-phase-c-verify.log`。

| 验证 | 结果 |
| --- | --- |
| 应用连续评审专项 | 28 项通过；另有恢复几何单元测试通过 |
| 仓储／恢复／迁移／声明／命令摘要 | 22 / 14 / 13 / 8 / 3 项通过 |
| Domain 绑定与适用性 | 2 项通过 |
| Rust v3 契约／Node 协议 | 20 / 30 项通过 |
| 原生图片证据与 Application 串接 | 全仓测试中通过，含 EXIF 正向、保存／部分存档／恢复／源变化、声明换内容拒绝 |
| UI 全量 | 132 个文件，1,142 项通过，1 项既有跳过 |
| 全仓质量与安全 | 格式、严格 Clippy、Rust workspace、架构边界、旧功能测试、构建、离线依赖和许可证检查通过 |
| 架构趋势 | 退出 0，46 项 warning；未调整基线或增加豁免 |

趋势日志为 `target/continuous-review-phase-c-trends.log`。46 项预警包含已有模块及新增应用／协议
校验函数的复杂度；通过趋势命令不等于没有架构债务。现有 Biome 配置迁移提示、测试 Canvas
环境提示、构建大 chunk 和依赖多版本 warning 未被隐瞒，也未为本阶段放宽规则。

恢复容量是累计限制：每项目最多 10,000 条、合计 64 MiB（包括已提交命令的恢复记录）；
索引备份另有 64 份／64 MiB 上限。达到边界会拒绝继续写入，不自动删除历史或输入。
这不能被宣传为无限持续编辑或大规模产品验收；后续接入 UI 前，保留／压缩策略须单独设计验证。

独立复验随后找出仓储 checkpoint 层的共享 Known basis 重复 IO：Application 去重发生太晚。
新增真实 PNG 仓储测试先观察到 100 个 Known 组读取同一依据 100 次，再修正为一次精确依据批次核验。
包含交错 Unknown 组、原分组和目标顺序保留、公开 archive 读取固定 4 次状态读取、伪造摘要和
已声明 PNG 损坏拒绝；不把旧依据或不同来源组合成新的产品语义。聚焦仓储 22／恢复 14／
存档时间顺序 1 项及严格 Clippy 复验通过。

## 检查点 C 结论与续接

最终实现提交为 `21edbf6`，之前的复审修正提交为 `7fae416`。
在最终实现再次运行完整 `pnpm verify:clean`，退出 0；日志为
`target/continuous-review-phase-c-verify-final.log`。表中全量测试和门禁均在最终实现保持通过，
包含新真实 PNG 共享依据回归。验证前后工作树干净；最终文档同步后另跑文档政策和 diff 检查。

独立复审者分别检查任务 11–12、任务 13、第一轮修正和 `7fae416..21edbf6` 最后修正，
确认全部已报告 Important 已关闭，无新的 Critical／Important；结论为完整门禁通过后可关闭 C。
复审者只读审查、核对测试和验证日志，没有编辑代码，也没有另行并行运行整套测试；
全仓执行证据来自主线程，不把日志复核说成另一份独立全量测试。

任务 11／12／13 分别为 `475677c`／`ef4df73`／`30cdeee`；检查点 C 完成，累计 13 / 23。
继续使用本记录开头的隔离工作区，从阶段 D 的 Task 14 开始（独立读取器的安全 IO／摘要），
随后 Task 15 当前／历史读取、Task 16 桌面对外边界。任务 14–23 尚未实施，不提前接 UI。
累计恢复容量、架构趋势与大历史负载限制保留如上，不因检查点通过而隐去。

原工作区 `/Users/abc/Project/Viewer` 再次核验为干净，仍为
`codex/image-annotation-review-workbench`／`d7abb9961f377ae257c3283ef7893218ec0c53f9`。
没有合并、推送、自动迁移真实工程或改写原始素材；现有 UI、桌面组装、Node reader 入口未切换。
当前不自动迁移任何旧索引，不把历史或存档解释为 Agent 已执行、问题已解决或素材已通过。
签名、公证、正式安装包、上架、公开发布和发售不属于开发初期的本轮任务或验收。
