{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Viewer main window with folder selection and scan-event subscription only",
  "windows": ["main"],
  "permissions": [
    "dialog:allow-open",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
