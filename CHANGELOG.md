# Viewer 版本记录

本文件记录可追溯的 Viewer 开发基线，不代表已经公开分发、签名、公证或正式发售的软件版本。
产品仍以源码开发构建持续迭代。

## 0.1.9 — 2026-09-11

### 改进与修复

- 评审标记线段全面改为解析式抗锯齿矢量描边，修复发丝线/串珠状等剖面异常；标记与徽章改为珊瑚红 + 白底彩环设计，GPU 颜色契约统一为线性编码（sRGB 输入经转换注入），评审意见栏重设计为卡片式布局。
- 修复系统重启后 APFS 卷设备号变化导致总览页评审角标与"已审阅"进度全部失效的问题：源检查以 blake3 内容指纹验证文件身份并自动重绑会话级实体 ID（显式重定位资产不受影响）。
- 总览页文件夹信息列由 184px 收窄至 112px（名称与路径同排、状态点、迷你进度条、返工数量精简为纯数字），目录树行高 22px、选中态改为白底浮起卡片；文件夹栏默认宽度收窄为 152px、拖拽下限放宽至 120px。
- 缩略图 `<img>` 增加 `decoding="async"`，降低滚动挂载时图像解码对绘制的阻塞。
- 视频打包测试新增 6 个脚本接入 `test:video:packaging`；v4 评审协议探测进入迁移检查分支。
- 新增性能优化实施方案（解码调度器、滚动取消与预取、内存压力联动、性能预算门禁），作为后续迭代设计基线。
- 同步 Cargo、Tauri、前端包和产品文档版本为 `0.1.9`，统一 Git 标签为 `v0.1.9`。本版本仍是源码开发基线，不涉及签名、公证、正式安装包、上架或发售。

## 0.1.8 — 2026-09-09

### 改进与修复

- 引入原生 wgpu/Metal 大图渲染及评审图层合成，加入分块加载、内存预算和原生输入路由。
- 修复预览根背景遮挡原生图像，以及系统缩略图 1 像素取整差异被误判为解码失败的问题。
- 补充分块边缘、图层合成、内存和原生预览验证；不代表全软件无缺陷声明。
- 同步 Cargo、Tauri、前端包和当前产品文档版本为 `0.1.8`，统一 Git 标签为 `v0.1.8`。
- 本版本仍是源码开发基线，不涉及签名、公证、正式安装包、上架或发售。

## 0.1.7 — 2026-08-31

### 新增

- 增加面向 AI 素材生产链的图片评审工作台：在大图中使用矩形、画笔或整图意见标注问题，
  支持同一素材的多条自然语言意见，以及保存后的编辑、重绘和删除。
- 将新建和兼容工程的主评审流程切换为 `viewer.review/3` 持续评审；当前意见保存后即可供
  外部 Agent 精确读取，不再要求用户逐张确认通过或先“完成本轮”。
- 增加完整 current、手动精确存档、部分存档后补保留、历史查看/继续/恢复、素材换版确认，
  以及可选返工依据声明；存档和声明均不表示 Agent 已执行、已修复或已建立 lineage。
- 增加 Agent 无关的 `read-current.mjs`、`read-history.mjs` 和 Rust 只读核心；读取必须选择
  精确 Review Stream 或历史记录，current 替换旧待办，history 不进入 actionable。

### 改进与修复

- 完成旧 v1/v2 数据的显式迁移和只读兼容、生产历史选择器目录、冷启动意见恢复及源版本
  失败关闭；旧字节与历史证据不会被静默重写。
- 修复图片评审导出 PNG 中第二处及后续标注轮廓变白的问题；隔离编号绘制状态并保持
  轮廓与白色编号可见，不修改原始素材或历史记录。
- 重构工作区协调、浏览、反馈和文件整理边界，减少 UI 与 Tauri/Rust 基础设施之间的直接耦合，
  同时保持原有图片、视频、搜索、整理和撤销行为。

### 协议、验证与开发边界

- 建立版本化持续评审状态、索引、存档、读取结果和 Agent 使用声明协议，并提供完整性、上限、
  原子提交、崩溃恢复、来源变更与历史身份保护。
- Rust→Node 多轮、协议、UI 接受场景、全仓门禁及受控原生四标注→重启→换版→存档→新意见
  流程通过；图片/视频预览、搜索、文件整理/撤销、导航和只读旧功能完成专项回归。
- 本版本仍是源码开发基线，不代表签名、公证、正式安装包、上架、公开发布或发售准备。

## 0.1.6 — 2026-08-18

### 新增

- 增加本地视频候选扫描、异步元数据探测、独立视频浏览分区和封面显示。
- 增加基于打包 libmpv/FFmpeg 运行时的原生视频表面，以及播放/暂停、定位、逐帧、音量、
  静音、0.5×–2×速度和全屏控制。
- 增加视频时间轴缩略图、1 GiB 有所有权标记的派生缓存、缓存统计和确认清理入口。

### 改进

- 将视频预览整理为紧凑、按视频比例约束的原生剧场布局，缩小工具栏和控制区对画面的占用。
- 区分交互预览定位与精确定位，合并连续 seek 和原生表面更新，减少拖动时间轴及窗口缩放时的阻塞。
- 将媒体命令移出主线程/AppKit 路径，并在暂停缩放结束后主动重绘画面。
- 对视频打开、封面、时间轴和原生表面使用会话/代次/请求身份门禁，丢弃迟到结果。
- 播放速率改为应用内圆角菜单，并统一视频预览控制的可见性、焦点和响应式访问方式。

### 修复

- 修复视频缩略图和主播放无法显示、首帧未就绪即开放控制、WebView 重载后项目失效等问题。
- 修复窗口缩放/全屏交接时的原生表面几何、暂停画面不重绘、透明孔径越界和生命周期计时器失效。
- 修复时间轴快速交互反馈迟钝、精确定位反馈被预览请求覆盖及原生命令队列竞争。
- 修复视频播放结束后再次点击播放无反应；现在会回到开头重新播放。
- 修复紧凑控制在小窗口中被遮挡、布局偏离确认设计和播放速率菜单使用浏览器原生外观的问题。

### 文档与工程

- 锁定 mpv `v0.41.0`、FFmpeg `n8.0` 及其 LGPL 配置、来源和哈希，并显式关闭媒体网络能力。
- 增加运行时打包/许可证审计、视频探测/缓存/播放服务测试、原生几何测试和开发验收证据。
- 建立 0.1.6 当前产品规格、用户指南、功能/格式参考、数据隐私、故障排除和维护文档。

## Earlier development history

0.1.5 及更早开发基线没有在此补写推测性的完整版本说明。需要追溯时请以 Git 标签和提交历史为准，
例如 `git log --oneline v0.1.4..v0.1.5`。
