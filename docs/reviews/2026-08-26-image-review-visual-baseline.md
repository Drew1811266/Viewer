# 图片评审工作台：首版视觉基准确认

> Status: Development evidence
>
> 确认日期：2026-08-26；当前开发版本：`0.1.6`。

## 用户确认

用户在查看工作台预览及同图四条意见截图后，接受了“当前界面作为首版视觉基准，继续完成后续验收”。
这确认了当前工作台的界面方向和布局，不等于全部场景、原生交互或最终验收已经通过。
后续应从这份确认继续，不要重复询问用户是否接受同一版界面。

- 对应实现：`2d68d3d8f5637c43ca1e7aa36ddf63c130f29174`。
- 展示状态：`RVW-17 / review-image-four-annotations`，视口 `1024x720`。
- 已确认截图：[保存的基准图](assets/review-workbench-approved-2026-08-26.jpg)。
- 格式：JPEG，1024×720；保留原始截图字节，不把 JPEG 转码称作原生无损截图。
- SHA-256：`8935ac8c6ed198ce598f4d04ad0b5a32c90c20781b03d5c718acdaed4bcde907`。

该截图随 Git 保存。其余八个场景及宽窗口状态由相同界面实现生成候选参考，并逐项验收；
不得宣称用户已逐张人工确认了尚未展示的截图，也不得把同次产品截图复制成参考后宣称独立回归通过。
首次建立基准与后续针对冻结基准的回归，必须在证据中区分。

## 首次完整代码验证（历史记录）

在上述实现提交、干净工作树上运行 `pnpm verify:clean`，退出码为 0，检查前后没有工作树漂移。
本机日志：`target/review-workbench-final-verification.log`（忽略的开发产物，不随 Git 保存）。

- 前端：130 个测试文件，1123 项通过、1 项跳过；静态检查及生产构建通过。
- Rust workspace：845 项通过、0 项失败、7 项按仓库既有环境条件忽略；格式和严格 Clippy 通过。
- 开放协议 28 项、手工评审轮次端到端 7 项通过。
- 政策、范围、架构依赖/契约、现有视频运行时契约与验证器测试通过。
- 安全边界、离线依赖禁用/许可/来源政策、npm 许可和视频运行时许可检查通过。
- 既有 Biome 配置弃用提示、JSDOM canvas 提示、构建体积提示以及依赖重复版本告警仍存在。
  上一检查点的 21 条架构趋势告警也未被消除；本次没有通过改阈值来清除告警。

这证明对应实现的自动化检查通过，不等于已运行那些被忽略的原生媒体验收，也不等于 Task 14 完成。
后续最终文档/实现提交仍需按计划重新运行最终验证。

## 已获许可的完整捕获与首版基准

用户随后明确允许使用项目现有的独立 Playwright 自动化，范围仅限本机验收页面。
内置浏览器曾返回 1372×900 的裁切截图；该结果保留为早期检查记录，不用于宽窗口通过结论。

首次完整捕获发现 RVW-23 在语义 ready 后仍短暂显示缩略图任务。提交
`80096563e8cd57430fba0c7092e0a8a212bc8b6d` 修正了验收场景的就绪时序：
连续两帧重新检查真实状态，不用固定等待或隐藏任务 UI。对应两项回归先失败后通过，
22 项聚焦测试、17 项捕获脚本测试与前端构建通过；产品布局未改变。

在该干净提交上，RVW-16–RVW-24 × 1024×720、1440×900 共 18 张完整 PNG
逐张检查后冻结为首版参考。参考附有 `baseline-provenance.json`，明确记录来源是
本轮已接受的界面实现，不是历史 atlas 已有参考，也不是用户逐张确认。

- 首次基准产物：`target/viewer-visual-acceptance/image-annotation-review-workbench/initial-baseline/80096563e8cd57430fba0c7092e0a8a212bc8b6d/`。
- 冻结参考：`target/atlas-product-migration-reference/09eb6894e16194f856934c227fa0ebc0112f8bec64916781153c3734bc039b6f/<viewport>/RVW-16..24/`。
- 随后独立重跑的成对证据：`target/viewer-visual-acceptance/image-annotation-review-workbench/paired-baseline/80096563e8cd57430fba0c7092e0a8a212bc8b6d/`。

成对捕获使用 `pnpm accept:visual`，18 项运行时检查通过，无页面或控制台错误。
每项保存 `product.png`、`reference.png`、`combined.png`、`manifest.json` 和 `console.json`，
尺寸、提交清洁状态及摘要均已核对；18 张并排图已人工检查，未发现布局、遮挡或焦点环回归。
其中 16 张逐像素相同；RVW-18 两种尺寸分别相差 11、34 个像素，联合检查未见内容或布局差异。
这些是忽略目录内的本机证据，丢失后必须重跑，不能仅凭路径宣称通过。

## 修复后的独立复核与视觉验收

首版基准建立不等于最终功能验收。随后跨层代码复核发现 8 项 Important 问题：
旧版区域意见完成、导出 PNG 方向、极小图片渲染、未保存编辑保护、文字输入快捷键归属、
连续画笔与重绘意图、撤销恢复后的编号顺序，以及 v2 资源包完整性校验。
这 8 项已集中修复于 `ee71acc14b32dc92dd5f6b4b2acd6d8e6291d403`，新增回归记录了
先失败、修复后通过的证据。一次独立聚焦复核确认全部处理，修复范围内没有新的
Critical/Important 发现；这不是整个 Task 14 完成的结论。

修复后首次捕获为 16/18：RVW-20 两种尺寸的旧就绪条件错误要求保存后仍处于画笔模式。
`43663129a46032e1c7ee2998ce7e783fd5388e26` 仅修正验收脚本和测试，分别验证绘制前画笔
状态与保存后浏览状态，并检查真实桥接快照中的路径确实改变、原 ID/文字/创建时间保留、
没有未保存候选或待处理任务。5 项回归先失败后通过，91 项验收单元测试及 UI 检查通过；
同一次聚焦复核也接受了这份增量。旧的两张失败截图不计为通过。

在干净 `4366312` 上重新执行完整 `pnpm accept:visual`，18/18 运行时检查通过。
证据根目录：
`target/viewer-visual-acceptance/image-annotation-review-workbench/final/43663129a46032e1c7ee2998ce7e783fd5388e26/`。
每项的完整 PNG 尺寸、提交清洁状态、SHA-256、页面/控制台零错误均已核对，18 张并排图
均已逐张检查，未发现裁切、标记漂移、意见栏遮挡、编辑器溢出或焦点回归。
200% 状态保持既有的意见栏折叠/滚动规则。冻结参考没有被替换。

| 对照结果 | 1024×720 差异像素 | 1440×900 差异像素 | 判断 |
| --- | --- | --- | --- |
| RVW-16、17、19、21、23、24 | 0 | 0 | 12 张逐像素相同 |
| RVW-18 保存失败 | 5207 | 5208 | 临时意见未保存时，已有意见的修改/删除按钮禁用；文字、焦点和几何保留 |
| RVW-20 重绘完成 | 558 | 558 | 保存后共享状态机回到浏览；选中编号、文字和重绘路径正确 |
| RVW-22 未保存离开确认 | 5207 | 5207 | 背景意见按钮同样禁用；确认框布局及“继续编辑”焦点不变 |

这些差异来自已核验的编辑保护/保存状态修复，不是新布局，也不是零像素差异的虚假通过。
视觉证据仍是实际 App 加内存后端夹具，不能替代原生高分辨率端到端操作。

## 剩余原生验收与文档边界

设计 §13.4 的真实高分辨率 30 张图片流程尚未执行：第 6 张保存两条路径、两个矩形，
返回网格、重开项目恢复、完成为 29 pass/1 revise/0 unreviewable，再由外部 Reader 验证。
当前开发版原生程序已构建并启动，但操作工具报告 Mac 锁定；已请求用户手动解锁。
尚未取得高分辨率图片目录。准备好的独立 30 图副本只有 560×373，不能称为高分辨率证据。

依照设计 §14，产品文档和最终完成状态仍待原生验收补齐后同步。当前保持 Active / in progress。
本次修复的完整测试、非阻塞告警及续接步骤见
[开发检查点](../progress/2026-08-26-image-annotation-review-workbench-checkpoint.md)。
Viewer 仍处开发初期；签名、Apple 公证、正式安装包、公开发布和发售均不属于本轮事项。
