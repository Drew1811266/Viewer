# 图片标注评审工作台：开发检查点

> 日期：2026-08-26。状态：Development evidence；Task 14 尚未完成。

## 续接位置

分支为 `codex/image-annotation-review-workbench`。继续执行
[已确认的实施计划](../superpowers/plans/2026-08-26-viewer-image-annotation-review-workbench.md)，
不要重新启动产品方向讨论。Tasks 1–13 已实现并逐项提交；Task 14 的功能检查和
验收场景已实现，视觉基准确认、完整视觉对照、产品文档同步及最终清洁工作树验证仍待完成。

Viewer 仍处于开发初期。代码签名、Apple 公证、正式安装包、公开发布和发售均不属于
本轮开发、验收、阻塞或收尾工作，也不是每次功能完成后的默认任务。

## 已完成提交

| Task | 内容 | 提交 |
| --- | --- | --- |
| 1 | 有效的整图、矩形、画笔 Anchor 与限制 | `14c8550` |
| 2 | 隔离 v1 协议编解码，保留历史兼容 | `4c0cb4a` |
| 3 | v2 开放评审协议 | `1cd6f92` |
| 4 | 带版本、摘要和安全位置的目录记录 | `cdab9fd` |
| 5 | 崩溃一致的评审包发布和恢复 | `bc047c1` |
| 6 | 受限标注 PNG 渲染 | `b64b177` |
| 7 | 首条意见原子建 Draft 与标注用例 | `a12d624` |
| 8 | Tauri DTO 与前端桥接 | `555d792` |
| 9 | 不依赖 Review 的复用图片视口 | `c35fd05` |
| 10 | 前端标注编辑状态机 | `e7eadab` |
| 11 | 图片标注工作台 | `d301852` |
| 12 | 工作区、固定范围和网格反馈集成 | `d11ba3d` |
| 13 | 外部 v1/v2 Reader 和端到端兼容 | `76e5d0c` |

## 本次续接：Task 14

新增 RVW-16–RVW-24 九个确定性验收场景，覆盖自动开始、同图四条意见、保存失败、
矩形编辑、画笔重绘、范围外只读、未保存离开保护、网格意见计数和 200% 缩放。
场景经过真实 App、工作台和协调器，只有后端桥接使用内存夹具，不是截图替身，也不等于原生验收。
权威场景目录共 128 项，其中 24 项为评审场景。A11Y-05 原有 200% 缩放保持不变。

浏览器检查发现并修复：

- 工具栏在窄窗口重叠；评审专用工具栏允许换行，不改变普通预览布局。
- 200% 缩放时逻辑宽度与窗口宽度不同；意见栏按真实工作台宽度折叠。
- CSS 缩放下指针坐标映射错误；在通用图片投影边界修正，并覆盖四种旋转。
- 编号与矩形手柄重叠，以及偏移后的编号在图片外无法拖动；只为已有矩形编辑
  开放图片外坐标投影，新建标注仍拒绝图片外起点，最终几何仍限制在图片范围内。
- 底部翻页与意见栏/图片重叠；为翻页留空间并按图片区居中。
- 未保存区域在输入或保存失败时消失；保留虚线草稿，失败时恢复文字输入焦点。
- 短视口的就地编辑器超出图片区；使用紧凑输入布局并允许编辑器内部滚动。
- 首条意见夹具的范围数不一致；RVW-16 从当前目录的 30 张图片建立固定名单，
  单元测试核对 proposal、完整成员名单和保存后的总数。其余既有 Draft 场景保留六个固定成员。

独立代码复核最初发现一项拖动回归和一项夹具不一致：前者已由失败测试、修复后测试及
浏览器复测确认修复；后者已补充失败测试并修正。没有遗留的已确认 Critical/Important 发现。
该复核不等于完整验收通过或可以合并。

## 已验证范围

- 前端静态检查、完整单元测试与生产构建通过。最近完整测试：130 个文件，1123 项通过，1 项跳过。
- 视觉验收脚本单元测试 17 项通过；这不是运行视觉截图对照脚本的结果。
- 开放协议测试 28 项、手工轮次端到端测试 7 项通过。
- Rust 评审 Domain、Application 的开始/修改/完成/目录、Infrastructure 的协议/仓库/素材、
  macOS 图片渲染和 Desktop 评审命令测试均通过。
- 架构依赖检查及跨层契约测试通过；政策检查 33 项通过，范围检查覆盖 48 条要求、13 条冻结决定。
- 架构趋势命令退出码为 0，最终复测有 21 条非阻塞告警，包括大文件、长函数、分支复杂度及
  测试/生产代码比例下降。没有改动阈值或把告警视作已经解决。
- Biome 的既有配置弃用提示、JSDOM canvas 提示和生产包大于 500 kB 的提示仍存在。

本轮未执行最终 `pnpm verify:clean`，也未把设计标为 `Implemented` 或计划标为 `Complete`。

## 浏览器证据与尚缺的视觉验收

证据根目录：
`target/viewer-visual-acceptance/image-annotation-review-workbench/browser-product-evidence/`。
每个 `<viewport>/RVW-<编号>/` 目录保存 `product.jpg` 和 `manifest.json`。
这些文件是忽略目录中的本机开发产物，不随 Git 提交；丢失时须重新捕获，不应只凭路径宣称通过。

九个场景在 `1024x720`、`1440x900` 下均达到语义 ready、无待完成 UI 任务，
且工具栏可见控件通过边界与互不重叠检查。1024 宽的产品截图已逐张检查。
另实测了 200% 下的新建标注、保存失败焦点和未保存离开保护，以及缩小后的编号拖动。

以下两项仍阻止 Task 14 Step 5 完成：

1. 没有找到已确认的 RVW-16–RVW-24 参考截图。现有 atlas 不包含这组新工作台状态，
   文字设计不是像素参考图。不得把本次产品截图复制成参考图后宣称比对通过。
2. 当前内置浏览器的 1440×900 DOM 视口正常，但截图返回 1372×900 JPEG，右侧被裁切；
   `fullPage`/显式裁切参数的替代尝试出现拼接异常。宽窗口只记运行时检查通过，完整截图未通过。

截图实际格式为 JPEG，文件后缀已如实使用 `.jpg`，并在 manifest 记录实际截图尺寸与限制。
当前没有有效的 `reference.png` / `combined.png` 成对证据，因此没有运行并宣称
`pnpm accept:visual` 全部通过，也没有把浏览器夹具结果描述为原生 macOS 端到端验证。

## 下一步

1. 请用户确认是否把当前工作台界面确认为首版视觉基准，或提供需要遵循的参考版本。
2. 解决宽窗口完整捕获问题，并完成两种尺寸下九个状态的真实参考/产品对照与验收台账。
   若需改用独立 Playwright CLI，先取得用户对浏览器工具切换的授权。
3. Step 4–5 的证据全部通过后，同步产品规格、使用指南、功能、快捷键、隐私、故障排查及变更记录；
   此前保留这些文档的现有产品事实，不提前宣称本轮全面完成。
4. 在最终实现/文档提交上执行完整质量、安全与 `pnpm verify:clean`，再更新设计和计划的最终状态。

不要顺手扩展视频时间段/区域标注、AI Agent 联网集成、浏览曝光统计或发布工作。
